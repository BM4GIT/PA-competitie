#ifndef BUTTON_H
#define BUTTON_N

#include <QObject>
#include <button.h>

class Button : public QObject
{
    Q_OBJECT

public:
    Button();
    ~Button();




    int m_pin;
    int m_id;
    bool m_blink;
    int m_id;
    bool m_blink;d:
void timerEvent(QTimerEvent *event);

private:
int m_pin;
int m_id;
bool m_blink;protected:
void timerEvent(QTimerEvent *event);

private:
int m_pin;protected:protected:protected:protected:
void timerEvent(QTimerEvent *event);

private:
int m_pin;
int m_id;
bool m_blink;
void timerEvent(QTimerEvent *event);

private:
int m_pin;
int m_id;
bool m_blink;
void timerEvent(QTimerEvent *event);

private:
int m_pin;
int m_id;
bool m_blink;
void timerEvent(QTimerEvent *event);

private:
int m_pin;
int m_id;
bool m_blink;
int m_id;
bool m_blink;
void timerEvent(QTimerEvent *event);

private:
int m_pin;
int m_id;
bool m_blink;protected:
void timerEvent(QTimerEvent *event);

private:
int m_pin;protected:protected:protected:protected:
void timerEvent(QTimerEvent *event);

private:
int m_pin;
int m_id;
bool m_blink;
void timerEvent(QTimerEvent *event);

private:
int m_pin;
int m_id;
bool m_blink;
void timerEvent(QTimerEvent *event);

private:
int m_pin;
int m_id;
bool m_blink;
void timerEvent(QTimerEvent *event);

private:
int m_pin;
int m_id;
bool m_blink;
int m_id;
bool m_blink;

bool m_blink;
void timerEvent(QTimerEvent *event);

private:
int m_pin;
int m_id;
bool m_blink;
int m_id;
bool m_blink;

bool m_blink;protected:protecteprotected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;protected:
    void timerEvent(QTimerEvent *event);

private:protected:
    void timerEvent(QTimerEvent *event);

private:protected:
    void timerEvent(QTimerEvent *event);

private:protected:
    void timerEvent(QTimerEvent *event);

private:protected:
    void timerEvent(QTimerEvent *event);

private:protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;protected:protected:protected:protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    int m_id;
    bool m_blink;
    int m_pin;
    int m_id;
    bool m_blink;protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;protected:protected:protected:protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    int m_id;
    bool m_blink;
    int m_pin;
    int m_id;
    bool m_blink;protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;protected:protected:protected:protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    int m_id;
    bool m_blink;
    int m_pin;
    int m_id;
    bool m_blink;protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;protected:protected:protected:protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    int m_id;
    bool m_blink;
    int m_pin;
    int m_id;
    bool m_blink;protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;protected:protected:protected:protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    int m_id;
    bool m_blink;
    int m_pin;
    int m_id;
    bool m_blink;protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;protected:protected:protected:protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    int m_id;
    bool m_blink;;protected:protecteprotected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;protected:
        void timerEvent(QTimerEvent *event);

    private:protected:
        void timerEvent(QTimerEvent *event);

    private:protected:
        void timerEvent(QTimerEvent *event);

    private:protected:
        void timerEvent(QTimerEvent *event);

    private:protected:
        void timerEvent(QTimerEvent *event);

    private:protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;protected:protected:protected:protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        int m_id;
        bool m_blink;
        int m_pin;
        int m_id;
        bool m_blink;protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;protected:protected:protected:protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        int m_id;
        bool m_blink;
        int m_pin;
        int m_id;
        bool m_blink;protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;protected:protected:protected:protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        int m_id;
        bool m_blink;
        int m_pin;
        int m_id;
        bool m_blink;protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;protected:protected:protected:protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        int m_id;
        bool m_blink;
        int m_pin;
        int m_id;
        bool m_blink;protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;protected:protected:protected:protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        int m_id;
        bool m_blink;
        int m_pin;
        int m_id;
        bool m_blink;protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;protected:protected:protected:protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        int m_id;
        bool m_blink;
        int m_id;
        bool m_blink;protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;protected:protected:protected:protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        int m_id;
        bool m_blink;d:
    void timerEvent(QTimerEvent *event);

    private:
    int m_pin;
    int m_id;
    bool m_blink;protected:
    void timerEvent(QTimerEvent *event);

    private:
    int m_pin;protected:protected:protected:protected:
    void timerEvent(QTimerEvent *event);

    private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

    private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

    private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

    private:
    int m_pin;
    int m_id;
    bool m_blink;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

    private:
    int m_pin;
    int m_id;
    bool m_blink;protected:
    void timerEvent(QTimerEvent *event);

    private:
    int m_pin;protected:protected:protected:protected:
    void timerEvent(QTimerEvent *event);

    private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

    private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

    private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

    private:
    int m_pin;
    int m_id;
    bool m_blink;
    int m_id;
    bool m_blink;

    int m_id;
    bool m_blink;protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;protected:protected:protected:protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    int m_id;
    bool m_blink;d:
void timerEvent(QTimerEvent *event);

private:
int m_pin;
int m_id;
bool m_blink;protected:
void timerEvent(QTimerEvent *event);

private:
int m_pin;protected:protected:protected:protected:
void timerEvent(QTimerEvent *event);

private:
int m_pin;
int m_id;
bool m_blink;
void timerEvent(QTimerEvent *event);

private:
int m_pin;
int m_id;
bool m_blink;
void timerEvent(QTimerEvent *event);

private:
int m_pin;
int m_id;
bool m_blink;
void timerEvent(QTimerEvent *event);

private:
int m_pin;
int m_id;
bool m_blink;
int m_id;
bool m_blink;
void timerEvent(QTimerEvent *event);

private:
int m_pin;
int m_id;
bool m_blink;protected:
void timerEvent(QTimerEvent *event);

private:
int m_pin;protected:protected:protected:protected:
void timerEvent(QTimerEvent *event);

private:
int m_pin;
int m_id;
bool m_blink;
void timerEvent(QTimerEvent *event);

private:
int m_pin;
int m_id;
bool m_blink;
void timerEvent(QTimerEvent *event);

private:
int m_pin;
int m_id;protected:protecteprotected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;protected:
    void timerEvent(QTimerEvent *event);

private:protected:
    void timerEvent(QTimerEvent *event);

private:protected:
    void timerEvent(QTimerEvent *event);

private:protected:
    void timerEvent(QTimerEvent *event);

private:protected:
    void timerEvent(QTimerEvent *event);

private:protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;protected:protected:protected:protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    int m_id;
    bool m_blink;
    int m_pin;
    int m_id;
    bool m_blink;protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;protected:protected:protected:protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    int m_id;
    bool m_blink;
    int m_pin;
    int m_id;
    bool m_blink;protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;protected:protected:protected:protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    int m_id;
    bool m_blink;
    int m_pin;
    int m_id;
    bool m_blink;protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;protected:protected:protected:protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    int m_id;
    bool m_blink;
    int m_pin;
    int m_id;
    bool m_blink;protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;protected:protected:protected:protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    int m_id;
    bool m_blink;
    int m_pin;
    int m_id;
    bool m_blink;protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;protected:protected:protected:protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    int m_id;
    bool m_blink;;protected:protecteprotected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;protected:
        void timerEvent(QTimerEvent *event);

    private:protected:
        void timerEvent(QTimerEvent *event);

    private:protected:
        void timerEvent(QTimerEvent *event);

    private:protected:
        void timerEvent(QTimerEvent *event);

    private:protected:
        void timerEvent(QTimerEvent *event);

    private:protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;protected:protected:protected:protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        int m_id;
        bool m_blink;
        int m_pin;
        int m_id;
        bool m_blink;protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;protected:protected:protected:protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        int m_id;
        bool m_blink;
        int m_pin;
        int m_id;
        bool m_blink;protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;protected:protected:protected:protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        int m_id;
        bool m_blink;
        int m_pin;
        int m_id;
        bool m_blink;protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;protected:protected:protected:protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        int m_id;
        bool m_blink;
        int m_pin;
        int m_id;
        bool m_blink;protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;protected:protected:protected:protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        int m_id;
        bool m_blink;
        int m_pin;
        int m_id;
        bool m_blink;protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;protected:protected:protected:protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        int m_id;
        bool m_blink;
        int m_id;
        bool m_blink;protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;protected:protected:protected:protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        int m_id;
        bool m_blink;d:
    void timerEvent(QTimerEvent *event);

    private:
    int m_pin;
    int m_id;
    bool m_blink;protected:
    void timerEvent(QTimerEvent *event);

    private:
    int m_pin;protected:protected:protected:protected:
    void timerEvent(QTimerEvent *event);

    private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

    private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

    private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

    private:
    int m_pin;
    int m_id;
    bool m_blink;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

    private:
    int m_pin;
    int m_id;
    bool m_blink;protected:
    void timerEvent(QTimerEvent *event);

    private:
    int m_pin;protected:protected:protected:protected:
    void timerEvent(QTimerEvent *event);

    private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

    private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

    private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

    private:
    int m_pin;
    int m_id;
    bool m_blink;
    int m_id;
    bool m_blink;

    int m_id;
    bool m_blink;protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;protected:protected:protected:protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    int m_id;
    bool m_blink;d:
void timerEvent(QTimerEvent *event);

private:
int m_pin;
int m_id;
bool m_blink;protected:
void timerEvent(QTimerEvent *event);

private:
int m_pin;protected:protected:protected:protected:
void timerEvent(QTimerEvent *event);

private:
int m_pin;
int m_id;
bool m_blink;
void timerEvent(QTimerEvent *event);

private:
int m_pin;
int m_id;
bool m_blink;
void timerEvent(QTimerEvent *event);

private:
int m_pin;
int m_id;
bool m_blink;
void timerEvent(QTimerEvent *event);

private:
int m_pin;
int m_id;
bool m_blink;
int m_id;
bool m_blink;
void timerEvent(QTimerEvent *event);

private:
int m_pin;
int m_id;
bool m_blink;protected:
void timerEvent(QTimerEvent *event);

private:
int m_pin;protected:protected:protected:protected:
void timerEvent(QTimerEvent *event);

private:
int m_pin;
int m_id;
bool m_blink;
void timerEvent(QTimerEvent *event);

private:
int m_pin;
int m_id;
bool m_blink;
void timerEvent(QTimerEvent *event);

private:
int m_pin;
int m_id;protected:protecteprotected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;protected:
    void timerEvent(QTimerEvent *event);

private:protected:
    void timerEvent(QTimerEvent *event);

private:protected:
    void timerEvent(QTimerEvent *event);

private:protected:
    void timerEvent(QTimerEvent *event);

private:protected:
    void timerEvent(QTimerEvent *event);

private:protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;protected:protected:protected:protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    int m_id;
    bool m_blink;
    int m_pin;
    int m_id;
    bool m_blink;protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;protected:protected:protected:protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    int m_id;
    bool m_blink;
    int m_pin;
    int m_id;
    bool m_blink;protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;protected:protected:protected:protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    int m_id;
    bool m_blink;
    int m_pin;
    int m_id;
    bool m_blink;protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;protected:protected:protected:protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    int m_id;
    bool m_blink;
    int m_pin;
    int m_id;
    bool m_blink;protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;protected:protected:protected:protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    int m_id;
    bool m_blink;
    int m_pin;
    int m_id;
    bool m_blink;protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;protected:protected:protected:protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    int m_id;
    bool m_blink;;protected:protecteprotected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;protected:
        void timerEvent(QTimerEvent *event);

    private:protected:
        void timerEvent(QTimerEvent *event);

    private:protected:
        void timerEvent(QTimerEvent *event);

    private:protected:
        void timerEvent(QTimerEvent *event);

    private:protected:
        void timerEvent(QTimerEvent *event);

    private:protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;protected:protected:protected:protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        int m_id;
        bool m_blink;
        int m_pin;
        int m_id;
        bool m_blink;protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;protected:protected:protected:protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        int m_id;
        bool m_blink;
        int m_pin;
        int m_id;
        bool m_blink;protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;protected:protected:protected:protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        int m_id;
        bool m_blink;
        int m_pin;
        int m_id;
        bool m_blink;protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;protected:protected:protected:protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        int m_id;
        bool m_blink;
        int m_pin;
        int m_id;
        bool m_blink;protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;protected:protected:protected:protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        int m_id;
        bool m_blink;
        int m_pin;
        int m_id;
        bool m_blink;protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;protected:protected:protected:protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        int m_id;
        bool m_blink;
        int m_id;
        bool m_blink;protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;protected:protected:protected:protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        int m_id;
        bool m_blink;d:
    void timerEvent(QTimerEvent *event);

    private:
    int m_pin;
    int m_id;
    bool m_blink;protected:
    void timerEvent(QTimerEvent *event);

    private:
    int m_pin;protected:protected:protected:protected:
    void timerEvent(QTimerEvent *event);

    private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

    private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

    private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

    private:
    int m_pin;
    int m_id;
    bool m_blink;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

    private:
    int m_pin;
    int m_id;
    bool m_blink;protected:
    void timerEvent(QTimerEvent *event);

    private:
    int m_pin;protected:protected:protected:protected:
    void timerEvent(QTimerEvent *event);

    private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

    private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

    private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

    private:
    int m_pin;
    int m_id;
    bool m_blink;
    int m_id;
    bool m_blink;

    int m_id;
    bool m_blink;protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;protected:protected:protected:protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    int m_id;
    bool m_blink;d:
void timerEvent(QTimerEvent *event);

private:
int m_pin;
int m_id;
bool m_blink;protected:
void timerEvent(QTimerEvent *event);

private:
int m_pin;protected:protected:protected:protected:
void timerEvent(QTimerEvent *event);

private:
int m_pin;
int m_id;
bool m_blink;
void timerEvent(QTimerEvent *event);

private:
int m_pin;
int m_id;
bool m_blink;
void timerEvent(QTimerEvent *event);

private:
int m_pin;
int m_id;
bool m_blink;
void timerEvent(QTimerEvent *event);

private:
int m_pin;
int m_id;
bool m_blink;
int m_id;
bool m_blink;
void timerEvent(QTimerEvent *event);

private:
int m_pin;
int m_id;
bool m_blink;protected:
void timerEvent(QTimerEvent *event);

private:
int m_pin;protected:protected:protected:protected:
void timerEvent(QTimerEvent *event);

private:
int m_pin;
int m_id;
bool m_blink;
void timerEvent(QTimerEvent *event);

private:
int m_pin;
int m_id;
bool m_blink;
void timerEvent(QTimerEvent *event);

private:
int m_pin;
int m_id;
bool m_blink;
void timerEvent(QTimerEvent *event);

private:
int m_pin;
int m_id;
bool m_blink;
int m_id;
bool m_blink;

bool m_blink;
void timerEvent(QTimerEvent *event);

private:
int m_pin;
int m_id;
bool m_blink;
int m_id;
bool m_blink;protected:protecteprotected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;protected:
    void timerEvent(QTimerEvent *event);

private:protected:
    void timerEvent(QTimerEvent *event);

private:protected:
    void timerEvent(QTimerEvent *event);

private:protected:
    void timerEvent(QTimerEvent *event);

private:protected:
    void timerEvent(QTimerEvent *event);

private:protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;protected:protected:protected:protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    int m_id;
    bool m_blink;
    int m_pin;
    int m_id;
    bool m_blink;protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;protected:protected:protected:protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    int m_id;
    bool m_blink;
    int m_pin;
    int m_id;
    bool m_blink;protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;protected:protected:protected:protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    int m_id;
    bool m_blink;
    int m_pin;
    int m_id;
    bool m_blink;protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;protected:protected:protected:protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    int m_id;
    bool m_blink;
    int m_pin;
    int m_id;
    bool m_blink;protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;protected:protected:protected:protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    int m_id;
    bool m_blink;
    int m_pin;
    int m_id;
    bool m_blink;protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;protected:protected:protected:protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    int m_id;
    bool m_blink;;protected:protecteprotected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;protected:
        void timerEvent(QTimerEvent *event);

    private:protected:
        void timerEvent(QTimerEvent *event);

    private:protected:
        void timerEvent(QTimerEvent *event);

    private:protected:
        void timerEvent(QTimerEvent *event);

    private:protected:
        void timerEvent(QTimerEvent *event);

    private:protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;protected:protected:protected:protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        int m_id;
        bool m_blink;
        int m_pin;
        int m_id;
        bool m_blink;protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;protected:protected:protected:protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        int m_id;
        bool m_blink;
        int m_pin;
        int m_id;
        bool m_blink;protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;protected:protected:protected:protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        int m_id;
        bool m_blink;
        int m_pin;
        int m_id;
        bool m_blink;protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;protected:protected:protected:protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        int m_id;
        bool m_blink;
        int m_pin;
        int m_id;
        bool m_blink;protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;protected:protected:protected:protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        int m_id;
        bool m_blink;
        int m_pin;
        int m_id;
        bool m_blink;protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;protected:protected:protected:protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        int m_id;
        bool m_blink;
        int m_id;
        bool m_blink;protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;protected:protected:protected:protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        int m_id;
        bool m_blink;d:
    void timerEvent(QTimerEvent *event);

    private:
    int m_pin;
    int m_id;
    bool m_blink;protected:
    void timerEvent(QTimerEvent *event);

    private:
    int m_pin;protected:protected:protected:protected:
    void timerEvent(QTimerEvent *event);

    private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

    private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

    private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

    private:
    int m_pin;
    int m_id;
    bool m_blink;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

    private:
    int m_pin;
    int m_id;
    bool m_blink;protected:
    void timerEvent(QTimerEvent *event);

    private:
    int m_pin;protected:protected:protected:protected:
    void timerEvent(QTimerEvent *event);

    private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

    private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

    private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

    private:
    int m_pin;
    int m_id;
    bool m_blink;
    int m_id;
    bool m_blink;

    int m_id;
    bool m_blink;protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;protected:protected:protected:protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    int m_id;
    bool m_blink;d:
void timerEvent(QTimerEvent *event);

private:
int m_pin;
int m_id;
bool m_blink;protected:
void timerEvent(QTimerEvent *event);

private:
int m_pin;protected:protected:protected:protected:
void timerEvent(QTimerEvent *event);

private:
int m_pin;
int m_id;
bool m_blink;
void timerEvent(QTimerEvent *event);

private:
int m_pin;
int m_id;
bool m_blink;
void timerEvent(QTimerEvent *event);

private:
int m_pin;
int m_id;
bool m_blink;
void timerEvent(QTimerEvent *event);

private:
int m_pin;
int m_id;
bool m_blink;
int m_id;
bool m_blink;
void timerEvent(QTimerEvent *event);

private:
int m_pin;
int m_id;
bool m_blink;protected:
void timerEvent(QTimerEvent *event);

private:
int m_pin;protected:protected:protected:protected:
void timerEvent(QTimerEvent *event);

private:
int m_pin;
int m_id;
bool m_blink;
void timerEvent(QTimerEvent *event);

private:
int m_pin;
int m_id;
bool m_blink;
void timerEvent(QTimerEvent *event);

private:
int m_pin;
int m_id;protected:protecteprotected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;protected:
    void timerEvent(QTimerEvent *event);

private:protected:
    void timerEvent(QTimerEvent *event);

private:protected:
    void timerEvent(QTimerEvent *event);

private:protected:
    void timerEvent(QTimerEvent *event);

private:protected:
    void timerEvent(QTimerEvent *event);

private:protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;protected:protected:protected:protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    int m_id;
    bool m_blink;
    int m_pin;
    int m_id;
    bool m_blink;protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;protected:protected:protected:protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    int m_id;
    bool m_blink;
    int m_pin;
    int m_id;
    bool m_blink;protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;protected:protected:protected:protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    int m_id;
    bool m_blink;
    int m_pin;
    int m_id;
    bool m_blink;protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;protected:protected:protected:protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    int m_id;
    bool m_blink;
    int m_pin;
    int m_id;
    bool m_blink;protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;protected:protected:protected:protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    int m_id;
    bool m_blink;
    int m_pin;
    int m_id;
    bool m_blink;protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;protected:protected:protected:protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    int m_id;
    bool m_blink;;protected:protecteprotected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;protected:
        void timerEvent(QTimerEvent *event);

    private:protected:
        void timerEvent(QTimerEvent *event);

    private:protected:
        void timerEvent(QTimerEvent *event);

    private:protected:
        void timerEvent(QTimerEvent *event);

    private:protected:
        void timerEvent(QTimerEvent *event);

    private:protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;protected:protected:protected:protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        int m_id;
        bool m_blink;
        int m_pin;
        int m_id;
        bool m_blink;protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;protected:protected:protected:protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        int m_id;
        bool m_blink;
        int m_pin;
        int m_id;
        bool m_blink;protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;protected:protected:protected:protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        int m_id;
        bool m_blink;
        int m_pin;
        int m_id;
        bool m_blink;protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;protected:protected:protected:protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        int m_id;
        bool m_blink;
        int m_pin;
        int m_id;
        bool m_blink;protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;protected:protected:protected:protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        int m_id;
        bool m_blink;
        int m_pin;
        int m_id;
        bool m_blink;protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;protected:protected:protected:protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        int m_id;
        bool m_blink;
        int m_id;
        bool m_blink;protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;protected:protected:protected:protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        int m_id;
        bool m_blink;d:
    void timerEvent(QTimerEvent *event);

    private:
    int m_pin;
    int m_id;
    bool m_blink;protected:
    void timerEvent(QTimerEvent *event);

    private:
    int m_pin;protected:protected:protected:protected:
    void timerEvent(QTimerEvent *event);

    private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

    private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

    private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

    private:
    int m_pin;
    int m_id;
    bool m_blink;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

    private:
    int m_pin;
    int m_id;
    bool m_blink;protected:
    void timerEvent(QTimerEvent *event);

    private:
    int m_pin;protected:protected:protected:protected:
    void timerEvent(QTimerEvent *event);

    private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

    private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

    private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

    private:
    int m_pin;
    int m_id;
    bool m_blink;
    int m_id;
    bool m_blink;

    int m_id;
    bool m_blink;protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;protected:protected:protected:protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    int m_id;
    bool m_blink;d:
void timerEvent(QTimerEvent *event);

private:
int m_pin;
int m_id;
bool m_blink;protected:
void timerEvent(QTimerEvent *event);

private:
int m_pin;protected:protected:protected:protected:
void timerEvent(QTimerEvent *event);

private:
int m_pin;
int m_id;
bool m_blink;
void timerEvent(QTimerEvent *event);

private:
int m_pin;
int m_id;
bool m_blink;
void timerEvent(QTimerEvent *event);

private:
int m_pin;
int m_id;
bool m_blink;
void timerEvent(QTimerEvent *event);

private:
int m_pin;
int m_id;
bool m_blink;
int m_id;
bool m_blink;
void timerEvent(QTimerEvent *event);

private:
int m_pin;
int m_id;
bool m_blink;protected:
void timerEvent(QTimerEvent *event);

private:
int m_pin;protected:protected:protected:protected:
void timerEvent(QTimerEvent *event);

private:
int m_pin;
int m_id;
bool m_blink;
void timerEvent(QTimerEvent *event);

private:
int m_pin;
int m_id;
bool m_blink;
void timerEvent(QTimerEvent *event);

private:
int m_pin;
int m_id;protected:protecteprotected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;protected:
    void timerEvent(QTimerEvent *event);

private:protected:
    void timerEvent(QTimerEvent *event);

private:protected:
    void timerEvent(QTimerEvent *event);

private:protected:
    void timerEvent(QTimerEvent *event);

private:protected:
    void timerEvent(QTimerEvent *event);

private:protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;protected:protected:protected:protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    int m_id;
    bool m_blink;
    int m_pin;
    int m_id;
    bool m_blink;protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;protected:protected:protected:protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    int m_id;
    bool m_blink;
    int m_pin;
    int m_id;
    bool m_blink;protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;protected:protected:protected:protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    int m_id;
    bool m_blink;
    int m_pin;
    int m_id;
    bool m_blink;protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;protected:protected:protected:protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    int m_id;
    bool m_blink;
    int m_pin;
    int m_id;
    bool m_blink;protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;protected:protected:protected:protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    int m_id;
    bool m_blink;
    int m_pin;
    int m_id;
    bool m_blink;protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;protected:protected:protected:protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    int m_id;
    bool m_blink;;protected:protecteprotected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;protected:
        void timerEvent(QTimerEvent *event);

    private:protected:
        void timerEvent(QTimerEvent *event);

    private:protected:
        void timerEvent(QTimerEvent *event);

    private:protected:
        void timerEvent(QTimerEvent *event);

    private:protected:
        void timerEvent(QTimerEvent *event);

    private:protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;protected:protected:protected:protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        int m_id;
        bool m_blink;
        int m_pin;
        int m_id;
        bool m_blink;protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;protected:protected:protected:protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        int m_id;
        bool m_blink;
        int m_pin;
        int m_id;
        bool m_blink;protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;protected:protected:protected:protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        int m_id;
        bool m_blink;
        int m_pin;
        int m_id;
        bool m_blink;protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;protected:protected:protected:protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        int m_id;
        bool m_blink;
        int m_pin;
        int m_id;
        bool m_blink;protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;protected:protected:protected:protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        int m_id;
        bool m_blink;
        int m_pin;
        int m_id;
        bool m_blink;protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;protected:protected:protected:protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        int m_id;
        bool m_blink;
        int m_id;
        bool m_blink;protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;protected:protected:protected:protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        int m_id;
        bool m_blink;d:
    void timerEvent(QTimerEvent *event);

    private:
    int m_pin;
    int m_id;
    bool m_blink;protected:
    void timerEvent(QTimerEvent *event);

    private:
    int m_pin;protected:protected:protected:protected:
    void timerEvent(QTimerEvent *event);

    private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

    private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

    private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

    private:
    int m_pin;
    int m_id;
    bool m_blink;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

    private:
    int m_pin;
    int m_id;
    bool m_blink;protected:
    void timerEvent(QTimerEvent *event);

    private:
    int m_pin;protected:protected:protected:protected:
    void timerEvent(QTimerEvent *event);

    private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

    private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

    private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

    private:
    int m_pin;
    int m_id;
    bool m_blink;
    int m_id;
    bool m_blink;

    int m_id;
    bool m_blink;protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;protected:protected:protected:protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    int m_id;
    bool m_blink;d:
void timerEvent(QTimerEvent *event);

private:
int m_pin;
int m_id;
bool m_blink;protected:
void timerEvent(QTimerEvent *event);

private:
int m_pin;protected:protected:protected:protected:
void timerEvent(QTimerEvent *event);

private:
int m_pin;
int m_id;
bool m_blink;
void timerEvent(QTimerEvent *event);

private:
int m_pin;
int m_id;
bool m_blink;
void timerEvent(QTimerEvent *event);

private:
int m_pin;
int m_id;
bool m_blink;
void timerEvent(QTimerEvent *event);

private:
int m_pin;
int m_id;
bool m_blink;
int m_id;
bool m_blink;
void timerEvent(QTimerEvent *event);

private:
int m_pin;
int m_id;
bool m_blink;protected:
void timerEvent(QTimerEvent *event);

private:
int m_pin;protected:protected:protected:protected:
void timerEvent(QTimerEvent *event);

private:
int m_pin;
int m_id;
bool m_blink;
void timerEvent(QTimerEvent *event);

private:
int m_pin;
int m_id;
bool m_blink;
void timerEvent(QTimerEvent *event);

private:
int m_pin;
int m_id;
bool m_blink;
void timerEvent(QTimerEvent *event);

private:
int m_pin;
int m_id;
bool m_blink;
int m_id;
bool m_blink;

bool m_blink;
void timerEvent(QTimerEvent *event);

private:
int m_pin;
int m_id;
bool m_blink;
int m_id;
bool m_blink;protected:protecteprotected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;protected:
    void timerEvent(QTimerEvent *event);

private:protected:
    void timerEvent(QTimerEvent *event);

private:protected:
    void timerEvent(QTimerEvent *event);

private:protected:
    void timerEvent(QTimerEvent *event);

private:protected:
    void timerEvent(QTimerEvent *event);

private:protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;protected:protected:protected:protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    int m_id;
    bool m_blink;
    int m_pin;
    int m_id;
    bool m_blink;protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;protected:protected:protected:protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    int m_id;
    bool m_blink;
    int m_pin;
    int m_id;
    bool m_blink;protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;protected:protected:protected:protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    int m_id;
    bool m_blink;
    int m_pin;
    int m_id;
    bool m_blink;protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;protected:protected:protected:protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    int m_id;
    bool m_blink;
    int m_pin;
    int m_id;
    bool m_blink;protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;protected:protected:protected:protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    int m_id;
    bool m_blink;
    int m_pin;
    int m_id;
    bool m_blink;protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;protected:protected:protected:protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    int m_id;
    bool m_blink;;protected:protecteprotected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;protected:
        void timerEvent(QTimerEvent *event);

    private:protected:
        void timerEvent(QTimerEvent *event);

    private:protected:
        void timerEvent(QTimerEvent *event);

    private:protected:
        void timerEvent(QTimerEvent *event);

    private:protected:
        void timerEvent(QTimerEvent *event);

    private:protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;protected:protected:protected:protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        int m_id;
        bool m_blink;
        int m_pin;
        int m_id;
        bool m_blink;protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;protected:protected:protected:protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        int m_id;
        bool m_blink;
        int m_pin;
        int m_id;
        bool m_blink;protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;protected:protected:protected:protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        int m_id;
        bool m_blink;
        int m_pin;
        int m_id;
        bool m_blink;protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;protected:protected:protected:protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        int m_id;
        bool m_blink;
        int m_pin;
        int m_id;
        bool m_blink;protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;protected:protected:protected:protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        int m_id;
        bool m_blink;
        int m_pin;
        int m_id;
        bool m_blink;protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;protected:protected:protected:protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        int m_id;
        bool m_blink;
        int m_id;
        bool m_blink;protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;protected:protected:protected:protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        int m_id;
        bool m_blink;d:
    void timerEvent(QTimerEvent *event);

    private:
    int m_pin;
    int m_id;
    bool m_blink;protected:
    void timerEvent(QTimerEvent *event);

    private:
    int m_pin;protected:protected:protected:protected:
    void timerEvent(QTimerEvent *event);

    private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

    private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

    private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

    private:
    int m_pin;
    int m_id;
    bool m_blink;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

    private:
    int m_pin;
    int m_id;
    bool m_blink;protected:
    void timerEvent(QTimerEvent *event);

    private:
    int m_pin;protected:protected:protected:protected:
    void timerEvent(QTimerEvent *event);

    private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

    private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

    private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

    private:
    int m_pin;
    int m_id;
    bool m_blink;
    int m_id;
    bool m_blink;

    int m_id;
    bool m_blink;protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;protected:protected:protected:protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    int m_id;
    bool m_blink;d:
void timerEvent(QTimerEvent *event);

private:
int m_pin;
int m_id;
bool m_blink;protected:
void timerEvent(QTimerEvent *event);

private:
int m_pin;protected:protected:protected:protected:
void timerEvent(QTimerEvent *event);

private:
int m_pin;
int m_id;
bool m_blink;
void timerEvent(QTimerEvent *event);

private:
int m_pin;
int m_id;
bool m_blink;
void timerEvent(QTimerEvent *event);

private:
int m_pin;
int m_id;
bool m_blink;
void timerEvent(QTimerEvent *event);

private:
int m_pin;
int m_id;
bool m_blink;
int m_id;
bool m_blink;
void timerEvent(QTimerEvent *event);

private:
int m_pin;
int m_id;
bool m_blink;protected:
void timerEvent(QTimerEvent *event);

private:
int m_pin;protected:protected:protected:protected:
void timerEvent(QTimerEvent *event);

private:
int m_pin;
int m_id;
bool m_blink;
void timerEvent(QTimerEvent *event);

private:
int m_pin;
int m_id;
bool m_blink;
void timerEvent(QTimerEvent *event);

private:
int m_pin;
int m_id;protected:protecteprotected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;protected:
    void timerEvent(QTimerEvent *event);

private:protected:
    void timerEvent(QTimerEvent *event);

private:protected:
    void timerEvent(QTimerEvent *event);

private:protected:
    void timerEvent(QTimerEvent *event);

private:protected:
    void timerEvent(QTimerEvent *event);

private:protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;protected:protected:protected:protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    int m_id;
    bool m_blink;
    int m_pin;
    int m_id;
    bool m_blink;protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;protected:protected:protected:protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    int m_id;
    bool m_blink;
    int m_pin;
    int m_id;
    bool m_blink;protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;protected:protected:protected:protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    int m_id;
    bool m_blink;
    int m_pin;
    int m_id;
    bool m_blink;protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;protected:protected:protected:protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    int m_id;
    bool m_blink;
    int m_pin;
    int m_id;
    bool m_blink;protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;protected:protected:protected:protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    int m_id;
    bool m_blink;
    int m_pin;
    int m_id;
    bool m_blink;protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;protected:protected:protected:protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    int m_id;
    bool m_blink;;protected:protecteprotected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;protected:
        void timerEvent(QTimerEvent *event);

    private:protected:
        void timerEvent(QTimerEvent *event);

    private:protected:
        void timerEvent(QTimerEvent *event);

    private:protected:
        void timerEvent(QTimerEvent *event);

    private:protected:
        void timerEvent(QTimerEvent *event);

    private:protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;protected:protected:protected:protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        int m_id;
        bool m_blink;
        int m_pin;
        int m_id;
        bool m_blink;protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;protected:protected:protected:protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        int m_id;
        bool m_blink;
        int m_pin;
        int m_id;
        bool m_blink;protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;protected:protected:protected:protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        int m_id;
        bool m_blink;
        int m_pin;
        int m_id;
        bool m_blink;protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;protected:protected:protected:protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        int m_id;
        bool m_blink;
        int m_pin;
        int m_id;
        bool m_blink;protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;protected:protected:protected:protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        int m_id;
        bool m_blink;
        int m_pin;
        int m_id;
        bool m_blink;protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;protected:protected:protected:protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        int m_id;
        bool m_blink;
        int m_id;
        bool m_blink;protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;protected:protected:protected:protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        int m_id;
        bool m_blink;d:
    void timerEvent(QTimerEvent *event);

    private:
    int m_pin;
    int m_id;
    bool m_blink;protected:
    void timerEvent(QTimerEvent *event);

    private:
    int m_pin;protected:protected:protected:protected:
    void timerEvent(QTimerEvent *event);

    private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

    private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

    private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

    private:
    int m_pin;
    int m_id;
    bool m_blink;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

    private:
    int m_pin;
    int m_id;
    bool m_blink;protected:
    void timerEvent(QTimerEvent *event);

    private:
    int m_pin;protected:protected:protected:protected:
    void timerEvent(QTimerEvent *event);

    private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

    private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

    private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

    private:
    int m_pin;
    int m_id;
    bool m_blink;
    int m_id;
    bool m_blink;

    int m_id;
    bool m_blink;protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;protected:protected:protected:protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    int m_id;
    bool m_blink;d:
void timerEvent(QTimerEvent *event);

private:
int m_pin;
int m_id;
bool m_blink;protected:
void timerEvent(QTimerEvent *event);

private:
int m_pin;protected:protected:protected:protected:
void timerEvent(QTimerEvent *event);

private:
int m_pin;
int m_id;
bool m_blink;
void timerEvent(QTimerEvent *event);

private:
int m_pin;
int m_id;
bool m_blink;
void timerEvent(QTimerEvent *event);

private:
int m_pin;
int m_id;
bool m_blink;
void timerEvent(QTimerEvent *event);

private:
int m_pin;
int m_id;
bool m_blink;
int m_id;
bool m_blink;
void timerEvent(QTimerEvent *event);

private:
int m_pin;
int m_id;
bool m_blink;protected:
void timerEvent(QTimerEvent *event);

private:
int m_pin;protected:protected:protected:protected:
void timerEvent(QTimerEvent *event);

private:
int m_pin;
int m_id;
bool m_blink;
void timerEvent(QTimerEvent *event);

private:
int m_pin;
int m_id;
bool m_blink;
void timerEvent(QTimerEvent *event);

private:
int m_pin;
int m_id;protected:protecteprotected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;protected:
    void timerEvent(QTimerEvent *event);

private:protected:
    void timerEvent(QTimerEvent *event);

private:protected:
    void timerEvent(QTimerEvent *event);

private:protected:
    void timerEvent(QTimerEvent *event);

private:protected:
    void timerEvent(QTimerEvent *event);

private:protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;protected:protected:protected:protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    int m_id;
    bool m_blink;
    int m_pin;
    int m_id;
    bool m_blink;protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;protected:protected:protected:protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    int m_id;
    bool m_blink;
    int m_pin;
    int m_id;
    bool m_blink;protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;protected:protected:protected:protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    int m_id;
    bool m_blink;
    int m_pin;
    int m_id;
    bool m_blink;protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;protected:protected:protected:protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    int m_id;
    bool m_blink;
    int m_pin;
    int m_id;
    bool m_blink;protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;protected:protected:protected:protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    int m_id;
    bool m_blink;
    int m_pin;
    int m_id;
    bool m_blink;protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;protected:protected:protected:protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    int m_id;
    bool m_blink;;protected:protecteprotected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;protected:
        void timerEvent(QTimerEvent *event);

    private:protected:
        void timerEvent(QTimerEvent *event);

    private:protected:
        void timerEvent(QTimerEvent *event);

    private:protected:
        void timerEvent(QTimerEvent *event);

    private:protected:
        void timerEvent(QTimerEvent *event);

    private:protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;protected:protected:protected:protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        int m_id;
        bool m_blink;
        int m_pin;
        int m_id;
        bool m_blink;protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;protected:protected:protected:protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        int m_id;
        bool m_blink;
        int m_pin;
        int m_id;
        bool m_blink;protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;protected:protected:protected:protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        int m_id;
        bool m_blink;
        int m_pin;
        int m_id;
        bool m_blink;protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;protected:protected:protected:protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        int m_id;
        bool m_blink;
        int m_pin;
        int m_id;
        bool m_blink;protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;protected:protected:protected:protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        int m_id;
        bool m_blink;
        int m_pin;
        int m_id;
        bool m_blink;protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;protected:protected:protected:protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        int m_id;
        bool m_blink;
        int m_id;
        bool m_blink;protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;protected:protected:protected:protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        int m_id;
        bool m_blink;d:
    void timerEvent(QTimerEvent *event);

    private:
    int m_pin;
    int m_id;
    bool m_blink;protected:
    void timerEvent(QTimerEvent *event);

    private:
    int m_pin;protected:protected:protected:protected:
    void timerEvent(QTimerEvent *event);

    private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

    private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

    private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

    private:
    int m_pin;
    int m_id;
    bool m_blink;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

    private:
    int m_pin;
    int m_id;
    bool m_blink;protected:
    void timerEvent(QTimerEvent *event);

    private:
    int m_pin;protected:protected:protected:protected:
    void timerEvent(QTimerEvent *event);

    private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

    private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

    private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

    private:
    int m_pin;
    int m_id;
    bool m_blink;
    int m_id;
    bool m_blink;

    int m_id;
    bool m_blink;protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;protected:protected:protected:protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    int m_id;
    bool m_blink;d:
void timerEvent(QTimerEvent *event);

private:
int m_pin;
int m_id;
bool m_blink;protected:
void timerEvent(QTimerEvent *event);

private:
int m_pin;protected:protected:protected:protected:
void timerEvent(QTimerEvent *event);

private:
int m_pin;
int m_id;
bool m_blink;
void timerEvent(QTimerEvent *event);

private:
int m_pin;
int m_id;
bool m_blink;
void timerEvent(QTimerEvent *event);

private:
int m_pin;
int m_id;
bool m_blink;
void timerEvent(QTimerEvent *event);

private:
int m_pin;
int m_id;
bool m_blink;
int m_id;
bool m_blink;
void timerEvent(QTimerEvent *event);

private:
int m_pin;
int m_id;
bool m_blink;protected:
void timerEvent(QTimerEvent *event);

private:
int m_pin;protected:protected:protected:protected:
void timerEvent(QTimerEvent *event);

private:
int m_pin;
int m_id;
bool m_blink;
void timerEvent(QTimerEvent *event);

private:
int m_pin;
int m_id;
bool m_blink;
void timerEvent(QTimerEvent *event);

private:
int m_pin;
int m_id;
bool m_blink;
void timerEvent(QTimerEvent *event);

private:
int m_pin;
int m_id;
bool m_blink;
int m_id;
bool m_blink;

bool m_blink;
void timerEvent(QTimerEvent *event);

private:
int m_pin;
int m_id;
bool m_blink;
int m_id;
bool m_blink;protected:protecteprotected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;protected:
    void timerEvent(QTimerEvent *event);

private:protected:
    void timerEvent(QTimerEvent *event);

private:protected:
    void timerEvent(QTimerEvent *event);

private:protected:
    void timerEvent(QTimerEvent *event);

private:protected:
    void timerEvent(QTimerEvent *event);

private:protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;protected:protected:protected:protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    int m_id;
    bool m_blink;
    int m_pin;
    int m_id;
    bool m_blink;protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;protected:protected:protected:protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    int m_id;
    bool m_blink;
    int m_pin;
    int m_id;
    bool m_blink;protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;protected:protected:protected:protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    int m_id;
    bool m_blink;
    int m_pin;
    int m_id;
    bool m_blink;protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;protected:protected:protected:protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    int m_id;
    bool m_blink;
    int m_pin;
    int m_id;
    bool m_blink;protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;protected:protected:protected:protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    int m_id;
    bool m_blink;
    int m_pin;
    int m_id;
    bool m_blink;protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;protected:protected:protected:protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    int m_id;
    bool m_blink;;protected:protecteprotected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;protected:
        void timerEvent(QTimerEvent *event);

    private:protected:
        void timerEvent(QTimerEvent *event);

    private:protected:
        void timerEvent(QTimerEvent *event);

    private:protected:
        void timerEvent(QTimerEvent *event);

    private:protected:
        void timerEvent(QTimerEvent *event);

    private:protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;protected:protected:protected:protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        int m_id;
        bool m_blink;
        int m_pin;
        int m_id;
        bool m_blink;protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;protected:protected:protected:protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        int m_id;
        bool m_blink;
        int m_pin;
        int m_id;
        bool m_blink;protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;protected:protected:protected:protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        int m_id;
        bool m_blink;
        int m_pin;
        int m_id;
        bool m_blink;protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;protected:protected:protected:protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        int m_id;
        bool m_blink;
        int m_pin;
        int m_id;
        bool m_blink;protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;protected:protected:protected:protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        int m_id;
        bool m_blink;
        int m_pin;
        int m_id;
        bool m_blink;protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;protected:protected:protected:protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        int m_id;
        bool m_blink;
        int m_id;
        bool m_blink;protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;protected:protected:protected:protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        int m_id;
        bool m_blink;d:
    void timerEvent(QTimerEvent *event);

    private:
    int m_pin;
    int m_id;
    bool m_blink;protected:
    void timerEvent(QTimerEvent *event);

    private:
    int m_pin;protected:protected:protected:protected:
    void timerEvent(QTimerEvent *event);

    private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

    private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

    private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

    private:
    int m_pin;
    int m_id;
    bool m_blink;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

    private:
    int m_pin;
    int m_id;
    bool m_blink;protected:
    void timerEvent(QTimerEvent *event);

    private:
    int m_pin;protected:protected:protected:protected:
    void timerEvent(QTimerEvent *event);

    private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

    private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

    private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

    private:
    int m_pin;
    int m_id;
    bool m_blink;
    int m_id;
    bool m_blink;

    int m_id;
    bool m_blink;protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;protected:protected:protected:protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    int m_id;
    bool m_blink;d:
void timerEvent(QTimerEvent *event);

private:
int m_pin;
int m_id;
bool m_blink;protected:
void timerEvent(QTimerEvent *event);

private:
int m_pin;protected:protected:protected:protected:
void timerEvent(QTimerEvent *event);

private:
int m_pin;
int m_id;
bool m_blink;
void timerEvent(QTimerEvent *event);

private:
int m_pin;
int m_id;
bool m_blink;
void timerEvent(QTimerEvent *event);

private:
int m_pin;
int m_id;
bool m_blink;
void timerEvent(QTimerEvent *event);

private:
int m_pin;
int m_id;
bool m_blink;
int m_id;
bool m_blink;
void timerEvent(QTimerEvent *event);

private:
int m_pin;
int m_id;
bool m_blink;protected:
void timerEvent(QTimerEvent *event);

private:
int m_pin;protected:protected:protected:protected:
void timerEvent(QTimerEvent *event);

private:
int m_pin;
int m_id;
bool m_blink;
void timerEvent(QTimerEvent *event);

private:
int m_pin;
int m_id;
bool m_blink;
void timerEvent(QTimerEvent *event);

private:
int m_pin;
int m_id;protected:protecteprotected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;protected:
    void timerEvent(QTimerEvent *event);

private:protected:
    void timerEvent(QTimerEvent *event);

private:protected:
    void timerEvent(QTimerEvent *event);

private:protected:
    void timerEvent(QTimerEvent *event);

private:protected:
    void timerEvent(QTimerEvent *event);

private:protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;protected:protected:protected:protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    int m_id;
    bool m_blink;
    int m_pin;
    int m_id;
    bool m_blink;protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;protected:protected:protected:protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    int m_id;
    bool m_blink;
    int m_pin;
    int m_id;
    bool m_blink;protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;protected:protected:protected:protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    int m_id;
    bool m_blink;
    int m_pin;
    int m_id;
    bool m_blink;protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;protected:protected:protected:protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    int m_id;
    bool m_blink;
    int m_pin;
    int m_id;
    bool m_blink;protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;protected:protected:protected:protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    int m_id;
    bool m_blink;
    int m_pin;
    int m_id;
    bool m_blink;protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;protected:protected:protected:protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    int m_id;
    bool m_blink;;protected:protecteprotected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;protected:
        void timerEvent(QTimerEvent *event);

    private:protected:
        void timerEvent(QTimerEvent *event);

    private:protected:
        void timerEvent(QTimerEvent *event);

    private:protected:
        void timerEvent(QTimerEvent *event);

    private:protected:
        void timerEvent(QTimerEvent *event);

    private:protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;protected:protected:protected:protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        int m_id;
        bool m_blink;
        int m_pin;
        int m_id;
        bool m_blink;protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;protected:protected:protected:protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        int m_id;
        bool m_blink;
        int m_pin;
        int m_id;
        bool m_blink;protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;protected:protected:protected:protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        int m_id;
        bool m_blink;
        int m_pin;
        int m_id;
        bool m_blink;protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;protected:protected:protected:protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        int m_id;
        bool m_blink;
        int m_pin;
        int m_id;
        bool m_blink;protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;protected:protected:protected:protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        int m_id;
        bool m_blink;
        int m_pin;
        int m_id;
        bool m_blink;protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;protected:protected:protected:protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        int m_id;
        bool m_blink;
        int m_id;
        bool m_blink;protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;protected:protected:protected:protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        int m_id;
        bool m_blink;d:
    void timerEvent(QTimerEvent *event);

    private:
    int m_pin;
    int m_id;
    bool m_blink;protected:
    void timerEvent(QTimerEvent *event);

    private:
    int m_pin;protected:protected:protected:protected:
    void timerEvent(QTimerEvent *event);

    private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

    private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

    private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

    private:
    int m_pin;
    int m_id;
    bool m_blink;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

    private:
    int m_pin;
    int m_id;
    bool m_blink;protected:
    void timerEvent(QTimerEvent *event);

    private:
    int m_pin;protected:protected:protected:protected:
    void timerEvent(QTimerEvent *event);

    private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

    private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

    private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

    private:
    int m_pin;
    int m_id;
    bool m_blink;
    int m_id;
    bool m_blink;

    int m_id;
    bool m_blink;protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;protected:protected:protected:protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    int m_id;
    bool m_blink;d:
void timerEvent(QTimerEvent *event);

private:
int m_pin;
int m_id;
bool m_blink;protected:
void timerEvent(QTimerEvent *event);

private:
int m_pin;protected:protected:protected:protected:
void timerEvent(QTimerEvent *event);

private:
int m_pin;
int m_id;
bool m_blink;
void timerEvent(QTimerEvent *event);

private:
int m_pin;
int m_id;
bool m_blink;
void timerEvent(QTimerEvent *event);

private:
int m_pin;
int m_id;
bool m_blink;
void timerEvent(QTimerEvent *event);

private:
int m_pin;
int m_id;
bool m_blink;
int m_id;
bool m_blink;
void timerEvent(QTimerEvent *event);

private:
int m_pin;
int m_id;
bool m_blink;protected:
void timerEvent(QTimerEvent *event);

private:
int m_pin;protected:protected:protected:protected:
void timerEvent(QTimerEvent *event);

private:
int m_pin;
int m_id;
bool m_blink;
void timerEvent(QTimerEvent *event);

private:
int m_pin;
int m_id;
bool m_blink;
void timerEvent(QTimerEvent *event);

private:
int m_pin;
int m_id;protected:protecteprotected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;protected:
    void timerEvent(QTimerEvent *event);

private:protected:
    void timerEvent(QTimerEvent *event);

private:protected:
    void timerEvent(QTimerEvent *event);

private:protected:
    void timerEvent(QTimerEvent *event);

private:protected:
    void timerEvent(QTimerEvent *event);

private:protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;protected:protected:protected:protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    int m_id;
    bool m_blink;
    int m_pin;
    int m_id;
    bool m_blink;protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;protected:protected:protected:protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    int m_id;
    bool m_blink;
    int m_pin;
    int m_id;
    bool m_blink;protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;protected:protected:protected:protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    int m_id;
    bool m_blink;
    int m_pin;
    int m_id;
    bool m_blink;protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;protected:protected:protected:protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    int m_id;
    bool m_blink;
    int m_pin;
    int m_id;
    bool m_blink;protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;protected:protected:protected:protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    int m_id;
    bool m_blink;
    int m_pin;
    int m_id;
    bool m_blink;protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;protected:protected:protected:protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    int m_id;
    bool m_blink;;protected:protecteprotected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;protected:
        void timerEvent(QTimerEvent *event);

    private:protected:
        void timerEvent(QTimerEvent *event);

    private:protected:
        void timerEvent(QTimerEvent *event);

    private:protected:
        void timerEvent(QTimerEvent *event);

    private:protected:
        void timerEvent(QTimerEvent *event);

    private:protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;protected:protected:protected:protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        int m_id;
        bool m_blink;
        int m_pin;
        int m_id;
        bool m_blink;protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;protected:protected:protected:protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        int m_id;
        bool m_blink;
        int m_pin;
        int m_id;
        bool m_blink;protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;protected:protected:protected:protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        int m_id;
        bool m_blink;
        int m_pin;
        int m_id;
        bool m_blink;protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;protected:protected:protected:protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        int m_id;
        bool m_blink;
        int m_pin;
        int m_id;
        bool m_blink;protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;protected:protected:protected:protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        int m_id;
        bool m_blink;
        int m_pin;
        int m_id;
        bool m_blink;protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;protected:protected:protected:protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        int m_id;
        bool m_blink;
        int m_id;
        bool m_blink;protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;protected:protected:protected:protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        int m_id;
        bool m_blink;d:
    void timerEvent(QTimerEvent *event);

    private:
    int m_pin;
    int m_id;
    bool m_blink;protected:
    void timerEvent(QTimerEvent *event);

    private:
    int m_pin;protected:protected:protected:protected:
    void timerEvent(QTimerEvent *event);

    private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

    private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

    private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

    private:
    int m_pin;
    int m_id;
    bool m_blink;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

    private:
    int m_pin;
    int m_id;
    bool m_blink;protected:
    void timerEvent(QTimerEvent *event);

    private:
    int m_pin;protected:protected:protected:protected:
    void timerEvent(QTimerEvent *event);

    private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

    private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

    private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

    private:
    int m_pin;
    int m_id;
    bool m_blink;
    int m_id;
    bool m_blink;

    int m_id;
    bool m_blink;protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;protected:protected:protected:protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    int m_id;
    bool m_blink;d:
void timerEvent(QTimerEvent *event);

private:
int m_pin;
int m_id;
bool m_blink;protected:
void timerEvent(QTimerEvent *event);

private:
int m_pin;protected:protected:protected:protected:
void timerEvent(QTimerEvent *event);

private:
int m_pin;
int m_id;
bool m_blink;
void timerEvent(QTimerEvent *event);

private:
int m_pin;
int m_id;
bool m_blink;
void timerEvent(QTimerEvent *event);

private:
int m_pin;
int m_id;
bool m_blink;
void timerEvent(QTimerEvent *event);

private:
int m_pin;
int m_id;
bool m_blink;
int m_id;
bool m_blink;
void timerEvent(QTimerEvent *event);

private:
int m_pin;
int m_id;
bool m_blink;protected:
void timerEvent(QTimerEvent *event);

private:
int m_pin;protected:protected:protected:protected:
void timerEvent(QTimerEvent *event);

private:
int m_pin;
int m_id;
bool m_blink;
void timerEvent(QTimerEvent *event);

private:
int m_pin;
int m_id;
bool m_blink;
void timerEvent(QTimerEvent *event);

private:
int m_pin;
int m_id;
bool m_blink;
void timerEvent(QTimerEvent *event);

private:
int m_pin;
int m_id;
bool m_blink;
int m_id;
bool m_blink;

bool m_blink;
void timerEvent(QTimerEvent *event);

private:
int m_pin;
int m_id;
bool m_blink;
int m_id;
bool m_blink;protected:protecteprotected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;protected:
    void timerEvent(QTimerEvent *event);

private:protected:
    void timerEvent(QTimerEvent *event);

private:protected:
    void timerEvent(QTimerEvent *event);

private:protected:
    void timerEvent(QTimerEvent *event);

private:protected:
    void timerEvent(QTimerEvent *event);

private:protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;protected:protected:protected:protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    int m_id;
    bool m_blink;
    int m_pin;
    int m_id;
    bool m_blink;protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;protected:protected:protected:protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    int m_id;
    bool m_blink;
    int m_pin;
    int m_id;
    bool m_blink;protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;protected:protected:protected:protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    int m_id;
    bool m_blink;
    int m_pin;
    int m_id;
    bool m_blink;protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;protected:protected:protected:protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    int m_id;
    bool m_blink;
    int m_pin;
    int m_id;
    bool m_blink;protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;protected:protected:protected:protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    int m_id;
    bool m_blink;
    int m_pin;
    int m_id;
    bool m_blink;protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;protected:protected:protected:protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    int m_id;
    bool m_blink;;protected:protecteprotected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;protected:
        void timerEvent(QTimerEvent *event);

    private:protected:
        void timerEvent(QTimerEvent *event);

    private:protected:
        void timerEvent(QTimerEvent *event);

    private:protected:
        void timerEvent(QTimerEvent *event);

    private:protected:
        void timerEvent(QTimerEvent *event);

    private:protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;protected:protected:protected:protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        int m_id;
        bool m_blink;
        int m_pin;
        int m_id;
        bool m_blink;protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;protected:protected:protected:protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        int m_id;
        bool m_blink;
        int m_pin;
        int m_id;
        bool m_blink;protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;protected:protected:protected:protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        int m_id;
        bool m_blink;
        int m_pin;
        int m_id;
        bool m_blink;protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;protected:protected:protected:protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        int m_id;
        bool m_blink;
        int m_pin;
        int m_id;
        bool m_blink;protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;protected:protected:protected:protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        int m_id;
        bool m_blink;
        int m_pin;
        int m_id;
        bool m_blink;protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;protected:protected:protected:protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        int m_id;
        bool m_blink;
        int m_id;
        bool m_blink;protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;protected:protected:protected:protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        int m_id;
        bool m_blink;d:
    void timerEvent(QTimerEvent *event);

    private:
    int m_pin;
    int m_id;
    bool m_blink;protected:
    void timerEvent(QTimerEvent *event);

    private:
    int m_pin;protected:protected:protected:protected:
    void timerEvent(QTimerEvent *event);

    private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

    private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

    private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

    private:
    int m_pin;
    int m_id;
    bool m_blink;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

    private:
    int m_pin;
    int m_id;
    bool m_blink;protected:
    void timerEvent(QTimerEvent *event);

    private:
    int m_pin;protected:protected:protected:protected:
    void timerEvent(QTimerEvent *event);

    private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

    private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

    private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

    private:
    int m_pin;
    int m_id;
    bool m_blink;
    int m_id;
    bool m_blink;

    int m_id;
    bool m_blink;protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;protected:protected:protected:protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    int m_id;
    bool m_blink;d:
void timerEvent(QTimerEvent *event);

private:
int m_pin;
int m_id;
bool m_blink;protected:
void timerEvent(QTimerEvent *event);

private:
int m_pin;protected:protected:protected:protected:
void timerEvent(QTimerEvent *event);

private:
int m_pin;
int m_id;
bool m_blink;
void timerEvent(QTimerEvent *event);

private:
int m_pin;
int m_id;
bool m_blink;
void timerEvent(QTimerEvent *event);

private:
int m_pin;
int m_id;
bool m_blink;
void timerEvent(QTimerEvent *event);

private:
int m_pin;
int m_id;
bool m_blink;
int m_id;
bool m_blink;
void timerEvent(QTimerEvent *event);

private:
int m_pin;
int m_id;
bool m_blink;protected:
void timerEvent(QTimerEvent *event);

private:
int m_pin;protected:protected:protected:protected:
void timerEvent(QTimerEvent *event);

private:
int m_pin;
int m_id;
bool m_blink;
void timerEvent(QTimerEvent *event);

private:
int m_pin;
int m_id;
bool m_blink;
void timerEvent(QTimerEvent *event);

private:
int m_pin;
int m_id;protected:protecteprotected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;protected:
    void timerEvent(QTimerEvent *event);

private:protected:
    void timerEvent(QTimerEvent *event);

private:protected:
    void timerEvent(QTimerEvent *event);

private:protected:
    void timerEvent(QTimerEvent *event);

private:protected:
    void timerEvent(QTimerEvent *event);

private:protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;protected:protected:protected:protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    int m_id;
    bool m_blink;
    int m_pin;
    int m_id;
    bool m_blink;protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;protected:protected:protected:protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    int m_id;
    bool m_blink;
    int m_pin;
    int m_id;
    bool m_blink;protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;protected:protected:protected:protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    int m_id;
    bool m_blink;
    int m_pin;
    int m_id;
    bool m_blink;protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;protected:protected:protected:protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    int m_id;
    bool m_blink;
    int m_pin;
    int m_id;
    bool m_blink;protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;protected:protected:protected:protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    int m_id;
    bool m_blink;
    int m_pin;
    int m_id;
    bool m_blink;protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;protected:protected:protected:protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    int m_id;
    bool m_blink;;protected:protecteprotected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;protected:
        void timerEvent(QTimerEvent *event);

    private:protected:
        void timerEvent(QTimerEvent *event);

    private:protected:
        void timerEvent(QTimerEvent *event);

    private:protected:
        void timerEvent(QTimerEvent *event);

    private:protected:
        void timerEvent(QTimerEvent *event);

    private:protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;protected:protected:protected:protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        int m_id;
        bool m_blink;
        int m_pin;
        int m_id;
        bool m_blink;protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;protected:protected:protected:protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        int m_id;
        bool m_blink;
        int m_pin;
        int m_id;
        bool m_blink;protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;protected:protected:protected:protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        int m_id;
        bool m_blink;
        int m_pin;
        int m_id;
        bool m_blink;protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;protected:protected:protected:protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        int m_id;
        bool m_blink;
        int m_pin;
        int m_id;
        bool m_blink;protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;protected:protected:protected:protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        int m_id;
        bool m_blink;
        int m_pin;
        int m_id;
        bool m_blink;protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;protected:protected:protected:protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        int m_id;
        bool m_blink;
        int m_id;
        bool m_blink;protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;protected:protected:protected:protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        int m_id;
        bool m_blink;d:
    void timerEvent(QTimerEvent *event);

    private:
    int m_pin;
    int m_id;
    bool m_blink;protected:
    void timerEvent(QTimerEvent *event);

    private:
    int m_pin;protected:protected:protected:protected:
    void timerEvent(QTimerEvent *event);

    private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

    private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

    private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

    private:
    int m_pin;
    int m_id;
    bool m_blink;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

    private:
    int m_pin;
    int m_id;
    bool m_blink;protected:
    void timerEvent(QTimerEvent *event);

    private:
    int m_pin;protected:protected:protected:protected:
    void timerEvent(QTimerEvent *event);

    private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

    private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

    private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

    private:
    int m_pin;
    int m_id;
    bool m_blink;
    int m_id;
    bool m_blink;

    int m_id;
    bool m_blink;protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;protected:protected:protected:protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    int m_id;
    bool m_blink;d:
void timerEvent(QTimerEvent *event);

private:
int m_pin;
int m_id;
bool m_blink;protected:
void timerEvent(QTimerEvent *event);

private:
int m_pin;protected:protected:protected:protected:
void timerEvent(QTimerEvent *event);

private:
int m_pin;
int m_id;
bool m_blink;
void timerEvent(QTimerEvent *event);

private:
int m_pin;
int m_id;
bool m_blink;
void timerEvent(QTimerEvent *event);

private:
int m_pin;
int m_id;
bool m_blink;
void timerEvent(QTimerEvent *event);

private:
int m_pin;
int m_id;
bool m_blink;
int m_id;
bool m_blink;
void timerEvent(QTimerEvent *event);

private:
int m_pin;
int m_id;
bool m_blink;protected:
void timerEvent(QTimerEvent *event);

private:
int m_pin;protected:protected:protected:protected:
void timerEvent(QTimerEvent *event);

private:
int m_pin;
int m_id;
bool m_blink;
void timerEvent(QTimerEvent *event);

private:
int m_pin;
int m_id;
bool m_blink;
void timerEvent(QTimerEvent *event);

private:
int m_pin;
int m_id;protected:protecteprotected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;protected:
    void timerEvent(QTimerEvent *event);

private:protected:
    void timerEvent(QTimerEvent *event);

private:protected:
    void timerEvent(QTimerEvent *event);

private:protected:
    void timerEvent(QTimerEvent *event);

private:protected:
    void timerEvent(QTimerEvent *event);

private:protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;protected:protected:protected:protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    int m_id;
    bool m_blink;
    int m_pin;
    int m_id;
    bool m_blink;protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;protected:protected:protected:protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    int m_id;
    bool m_blink;
    int m_pin;
    int m_id;
    bool m_blink;protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;protected:protected:protected:protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    int m_id;
    bool m_blink;
    int m_pin;
    int m_id;
    bool m_blink;protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;protected:protected:protected:protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    int m_id;
    bool m_blink;
    int m_pin;
    int m_id;
    bool m_blink;protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;protected:protected:protected:protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    int m_id;
    bool m_blink;
    int m_pin;
    int m_id;
    bool m_blink;protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;protected:protected:protected:protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    int m_id;
    bool m_blink;;protected:protecteprotected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;protected:
        void timerEvent(QTimerEvent *event);

    private:protected:
        void timerEvent(QTimerEvent *event);

    private:protected:
        void timerEvent(QTimerEvent *event);

    private:protected:
        void timerEvent(QTimerEvent *event);

    private:protected:
        void timerEvent(QTimerEvent *event);

    private:protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;protected:protected:protected:protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        int m_id;
        bool m_blink;
        int m_pin;
        int m_id;
        bool m_blink;protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;protected:protected:protected:protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        int m_id;
        bool m_blink;
        int m_pin;
        int m_id;
        bool m_blink;protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;protected:protected:protected:protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        int m_id;
        bool m_blink;
        int m_pin;
        int m_id;
        bool m_blink;protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;protected:protected:protected:protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        int m_id;
        bool m_blink;
        int m_pin;
        int m_id;
        bool m_blink;protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;protected:protected:protected:protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        int m_id;
        bool m_blink;
        int m_pin;
        int m_id;
        bool m_blink;protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;protected:protected:protected:protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        int m_id;
        bool m_blink;
        int m_id;
        bool m_blink;protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;protected:protected:protected:protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        int m_id;
        bool m_blink;d:
    void timerEvent(QTimerEvent *event);

    private:
    int m_pin;
    int m_id;
    bool m_blink;protected:
    void timerEvent(QTimerEvent *event);

    private:
    int m_pin;protected:protected:protected:protected:
    void timerEvent(QTimerEvent *event);

    private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

    private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

    private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

    private:
    int m_pin;
    int m_id;
    bool m_blink;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

    private:
    int m_pin;
    int m_id;
    bool m_blink;protected:
    void timerEvent(QTimerEvent *event);

    private:
    int m_pin;protected:protected:protected:protected:
    void timerEvent(QTimerEvent *event);

    private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

    private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

    private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

    private:
    int m_pin;
    int m_id;
    bool m_blink;
    int m_id;
    bool m_blink;

    int m_id;
    bool m_blink;protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;protected:protected:protected:protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    int m_id;
    bool m_blink;d:
void timerEvent(QTimerEvent *event);

private:
int m_pin;
int m_id;
bool m_blink;protected:
void timerEvent(QTimerEvent *event);

private:
int m_pin;protected:protected:protected:protected:
void timerEvent(QTimerEvent *event);

private:
int m_pin;
int m_id;
bool m_blink;
void timerEvent(QTimerEvent *event);

private:
int m_pin;
int m_id;
bool m_blink;
void timerEvent(QTimerEvent *event);

private:
int m_pin;
int m_id;
bool m_blink;
void timerEvent(QTimerEvent *event);

private:
int m_pin;
int m_id;
bool m_blink;
int m_id;
bool m_blink;
void timerEvent(QTimerEvent *event);

private:
int m_pin;
int m_id;
bool m_blink;protected:
void timerEvent(QTimerEvent *event);

private:
int m_pin;protected:protected:protected:protected:
void timerEvent(QTimerEvent *event);

private:
int m_pin;
int m_id;
bool m_blink;
void timerEvent(QTimerEvent *event);

private:
int m_pin;
int m_id;
bool m_blink;
void timerEvent(QTimerEvent *event);

private:
int m_pin;
int m_id;
bool m_blink;
void timerEvent(QTimerEvent *event);

private:
int m_pin;
int m_id;
bool m_blink;
int m_id;
bool m_blink;

bool m_blink;
void timerEvent(QTimerEvent *event);

private:
int m_pin;
int m_id;
bool m_blink;
int m_id;
bool m_blink;protected:protecteprotected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;protected:
    void timerEvent(QTimerEvent *event);

private:protected:
    void timerEvent(QTimerEvent *event);

private:protected:
    void timerEvent(QTimerEvent *event);

private:protected:
    void timerEvent(QTimerEvent *event);

private:protected:
    void timerEvent(QTimerEvent *event);

private:protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;protected:protected:protected:protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    int m_id;
    bool m_blink;
    int m_pin;
    int m_id;
    bool m_blink;protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;protected:protected:protected:protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    int m_id;
    bool m_blink;
    int m_pin;
    int m_id;
    bool m_blink;protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;protected:protected:protected:protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    int m_id;
    bool m_blink;
    int m_pin;
    int m_id;
    bool m_blink;protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;protected:protected:protected:protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    int m_id;
    bool m_blink;
    int m_pin;
    int m_id;
    bool m_blink;protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;protected:protected:protected:protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    int m_id;
    bool m_blink;
    int m_pin;
    int m_id;
    bool m_blink;protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;protected:protected:protected:protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    int m_id;
    bool m_blink;;protected:protecteprotected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;protected:
        void timerEvent(QTimerEvent *event);

    private:protected:
        void timerEvent(QTimerEvent *event);

    private:protected:
        void timerEvent(QTimerEvent *event);

    private:protected:
        void timerEvent(QTimerEvent *event);

    private:protected:
        void timerEvent(QTimerEvent *event);

    private:protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;protected:protected:protected:protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        int m_id;
        bool m_blink;
        int m_pin;
        int m_id;
        bool m_blink;protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;protected:protected:protected:protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        int m_id;
        bool m_blink;
        int m_pin;
        int m_id;
        bool m_blink;protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;protected:protected:protected:protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        int m_id;
        bool m_blink;
        int m_pin;
        int m_id;
        bool m_blink;protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;protected:protected:protected:protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        int m_id;
        bool m_blink;
        int m_pin;
        int m_id;
        bool m_blink;protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;protected:protected:protected:protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        int m_id;
        bool m_blink;
        int m_pin;
        int m_id;
        bool m_blink;protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;protected:protected:protected:protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        int m_id;
        bool m_blink;
        int m_id;
        bool m_blink;protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;protected:protected:protected:protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        int m_id;
        bool m_blink;d:
    void timerEvent(QTimerEvent *event);

    private:
    int m_pin;
    int m_id;
    bool m_blink;protected:
    void timerEvent(QTimerEvent *event);

    private:
    int m_pin;protected:protected:protected:protected:
    void timerEvent(QTimerEvent *event);

    private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

    private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

    private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

    private:
    int m_pin;
    int m_id;
    bool m_blink;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

    private:
    int m_pin;
    int m_id;
    bool m_blink;protected:
    void timerEvent(QTimerEvent *event);

    private:
    int m_pin;protected:protected:protected:protected:
    void timerEvent(QTimerEvent *event);

    private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

    private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

    private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

    private:
    int m_pin;
    int m_id;
    bool m_blink;
    int m_id;
    bool m_blink;

    int m_id;
    bool m_blink;protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;protected:protected:protected:protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    int m_id;
    bool m_blink;d:
void timerEvent(QTimerEvent *event);

private:
int m_pin;
int m_id;
bool m_blink;protected:
void timerEvent(QTimerEvent *event);

private:
int m_pin;protected:protected:protected:protected:
void timerEvent(QTimerEvent *event);

private:
int m_pin;
int m_id;
bool m_blink;
void timerEvent(QTimerEvent *event);

private:
int m_pin;
int m_id;
bool m_blink;
void timerEvent(QTimerEvent *event);

private:
int m_pin;
int m_id;
bool m_blink;
void timerEvent(QTimerEvent *event);

private:
int m_pin;
int m_id;
bool m_blink;
int m_id;
bool m_blink;
void timerEvent(QTimerEvent *event);

private:
int m_pin;
int m_id;
bool m_blink;protected:
void timerEvent(QTimerEvent *event);

private:
int m_pin;protected:protected:protected:protected:
void timerEvent(QTimerEvent *event);

private:
int m_pin;
int m_id;
bool m_blink;
void timerEvent(QTimerEvent *event);

private:
int m_pin;
int m_id;
bool m_blink;
void timerEvent(QTimerEvent *event);

private:
int m_pin;
int m_id;protected:protecteprotected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;protected:
    void timerEvent(QTimerEvent *event);

private:protected:
    void timerEvent(QTimerEvent *event);

private:protected:
    void timerEvent(QTimerEvent *event);

private:protected:
    void timerEvent(QTimerEvent *event);

private:protected:
    void timerEvent(QTimerEvent *event);

private:protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;protected:protected:protected:protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    int m_id;
    bool m_blink;
    int m_pin;
    int m_id;
    bool m_blink;protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;protected:protected:protected:protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    int m_id;
    bool m_blink;
    int m_pin;
    int m_id;
    bool m_blink;protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;protected:protected:protected:protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    int m_id;
    bool m_blink;
    int m_pin;
    int m_id;
    bool m_blink;protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;protected:protected:protected:protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    int m_id;
    bool m_blink;
    int m_pin;
    int m_id;
    bool m_blink;protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;protected:protected:protected:protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    int m_id;
    bool m_blink;
    int m_pin;
    int m_id;
    bool m_blink;protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;protected:protected:protected:protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    int m_id;
    bool m_blink;;protected:protecteprotected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;protected:
        void timerEvent(QTimerEvent *event);

    private:protected:
        void timerEvent(QTimerEvent *event);

    private:protected:
        void timerEvent(QTimerEvent *event);

    private:protected:
        void timerEvent(QTimerEvent *event);

    private:protected:
        void timerEvent(QTimerEvent *event);

    private:protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;protected:protected:protected:protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        int m_id;
        bool m_blink;
        int m_pin;
        int m_id;
        bool m_blink;protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;protected:protected:protected:protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        int m_id;
        bool m_blink;
        int m_pin;
        int m_id;
        bool m_blink;protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;protected:protected:protected:protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        int m_id;
        bool m_blink;
        int m_pin;
        int m_id;
        bool m_blink;protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;protected:protected:protected:protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        int m_id;
        bool m_blink;
        int m_pin;
        int m_id;
        bool m_blink;protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;protected:protected:protected:protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        int m_id;
        bool m_blink;
        int m_pin;
        int m_id;
        bool m_blink;protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;protected:protected:protected:protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        int m_id;
        bool m_blink;
        int m_id;
        bool m_blink;protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;protected:protected:protected:protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        int m_id;
        bool m_blink;d:
    void timerEvent(QTimerEvent *event);

    private:
    int m_pin;
    int m_id;
    bool m_blink;protected:
    void timerEvent(QTimerEvent *event);

    private:
    int m_pin;protected:protected:protected:protected:
    void timerEvent(QTimerEvent *event);

    private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

    private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

    private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

    private:
    int m_pin;
    int m_id;
    bool m_blink;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

    private:
    int m_pin;
    int m_id;
    bool m_blink;protected:
    void timerEvent(QTimerEvent *event);

    private:
    int m_pin;protected:protected:protected:protected:
    void timerEvent(QTimerEvent *event);

    private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

    private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

    private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

    private:
    int m_pin;
    int m_id;
    bool m_blink;
    int m_id;
    bool m_blink;

    int m_id;
    bool m_blink;protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;protected:protected:protected:protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    int m_id;
    bool m_blink;d:
void timerEvent(QTimerEvent *event);

private:
int m_pin;
int m_id;
bool m_blink;protected:
void timerEvent(QTimerEvent *event);

private:
int m_pin;protected:protected:protected:protected:
void timerEvent(QTimerEvent *event);

private:
int m_pin;
int m_id;
bool m_blink;
void timerEvent(QTimerEvent *event);

private:
int m_pin;
int m_id;
bool m_blink;
void timerEvent(QTimerEvent *event);

private:
int m_pin;
int m_id;
bool m_blink;
void timerEvent(QTimerEvent *event);

private:
int m_pin;
int m_id;
bool m_blink;
int m_id;
bool m_blink;
void timerEvent(QTimerEvent *event);

private:
int m_pin;
int m_id;
bool m_blink;protected:
void timerEvent(QTimerEvent *event);

private:
int m_pin;protected:protected:protected:protected:
void timerEvent(QTimerEvent *event);

private:
int m_pin;
int m_id;
bool m_blink;
void timerEvent(QTimerEvent *event);

private:
int m_pin;
int m_id;
bool m_blink;
void timerEvent(QTimerEvent *event);

private:
int m_pin;
int m_id;protected:protecteprotected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;protected:
    void timerEvent(QTimerEvent *event);

private:protected:
    void timerEvent(QTimerEvent *event);

private:protected:
    void timerEvent(QTimerEvent *event);

private:protected:
    void timerEvent(QTimerEvent *event);

private:protected:
    void timerEvent(QTimerEvent *event);

private:protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;protected:protected:protected:protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    int m_id;
    bool m_blink;
    int m_pin;
    int m_id;
    bool m_blink;protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;protected:protected:protected:protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    int m_id;
    bool m_blink;
    int m_pin;
    int m_id;
    bool m_blink;protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;protected:protected:protected:protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    int m_id;
    bool m_blink;
    int m_pin;
    int m_id;
    bool m_blink;protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;protected:protected:protected:protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    int m_id;
    bool m_blink;
    int m_pin;
    int m_id;
    bool m_blink;protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;protected:protected:protected:protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    int m_id;
    bool m_blink;
    int m_pin;
    int m_id;
    bool m_blink;protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;protected:protected:protected:protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    int m_id;
    bool m_blink;;protected:protecteprotected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;protected:
        void timerEvent(QTimerEvent *event);

    private:protected:
        void timerEvent(QTimerEvent *event);

    private:protected:
        void timerEvent(QTimerEvent *event);

    private:protected:
        void timerEvent(QTimerEvent *event);

    private:protected:
        void timerEvent(QTimerEvent *event);

    private:protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;protected:protected:protected:protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        int m_id;
        bool m_blink;
        int m_pin;
        int m_id;
        bool m_blink;protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;protected:protected:protected:protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        int m_id;
        bool m_blink;
        int m_pin;
        int m_id;
        bool m_blink;protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;protected:protected:protected:protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        int m_id;
        bool m_blink;
        int m_pin;
        int m_id;
        bool m_blink;protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;protected:protected:protected:protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        int m_id;
        bool m_blink;
        int m_pin;
        int m_id;
        bool m_blink;protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;protected:protected:protected:protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        int m_id;
        bool m_blink;
        int m_pin;
        int m_id;
        bool m_blink;protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;protected:protected:protected:protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        int m_id;
        bool m_blink;
        int m_id;
        bool m_blink;protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;protected:protected:protected:protected:
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        void timerEvent(QTimerEvent *event);

    private:
        int m_pin;
        int m_id;
        bool m_blink;
        int m_id;
        bool m_blink;d:
    void timerEvent(QTimerEvent *event);

    private:
    int m_pin;
    int m_id;
    bool m_blink;protected:
    void timerEvent(QTimerEvent *event);

    private:
    int m_pin;protected:protected:protected:protected:
    void timerEvent(QTimerEvent *event);

    private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

    private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

    private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

    private:
    int m_pin;
    int m_id;
    bool m_blink;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

    private:
    int m_pin;
    int m_id;
    bool m_blink;protected:
    void timerEvent(QTimerEvent *event);

    private:
    int m_pin;protected:protected:protected:protected:
    void timerEvent(QTimerEvent *event);

    private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

    private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

    private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

    private:
    int m_pin;
    int m_id;
    bool m_blink;
    int m_id;
    bool m_blink;

    int m_id;
    bool m_blink;protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;protected:protected:protected:protected:
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    void timerEvent(QTimerEvent *event);

private:
    int m_pin;
    int m_id;
    bool m_blink;
    int m_id;
    bool m_blink;d:
void timerEvent(QTimerEvent *event);

private:
int m_pin;
int m_id;
bool m_blink;protected:
void timerEvent(QTimerEvent *event);

private:
int m_pin;protected:protected:protected:protected:
void timerEvent(QTimerEvent *event);

private:
int m_pin;
int m_id;
bool m_blink;
void timerEvent(QTimerEvent *event);

private:
int m_pin;
int m_id;
bool m_blink;
void timerEvent(QTimerEvent *event);

private:
int m_pin;
int m_id;
bool m_blink;
void timerEvent(QTimerEvent *event);

private:
int m_pin;
int m_id;
bool m_blink;
int m_id;
bool m_blink;
void timerEvent(QTimerEvent *event);

private:
int m_pin;
int m_id;
bool m_blink;protected:
void timerEvent(QTimerEvent *event);

private:
int m_pin;protected:protected:protected:protected:
void timerEvent(QTimerEvent *event);

private:
int m_pin;
int m_id;
bool m_blink;
void timerEvent(QTimerEvent *event);

private:
int m_pin;
int m_id;
bool m_blink;
void timerEvent(QTimerEvent *event);

private:
int m_pin;
int m_id;
bool m_blink;
void timerEvent(QTimerEvent *event);

private:
int m_pin;
int m_id;
bool m_blink;
int m_id;
bool m_blink;

bool m_blink;
void timerEvent(QTimerEvent *event);

private:
int m_pin;
int m_id;
bool m_blink;
int m_id;
bool m_blink;

bool m_blink;
void timerEvent(QTimerEvent *event);

private:
int m_pin;
int m_id;
bool m_blink;
int m_id;
bool m_blink;


bool m_blink;
void timerEvent(QTimerEvent *event);

private:
int m_pin;
int m_id;
bool m_blink;
int m_id;
bool m_blink;


bool m_blink;
void timerEvent(QTimerEvent *event);

private:
int m_pin;
int m_id;
bool m_blink;
int m_id;
bool m_blink;


bool m_blink;
void timerEvent(QTimerEvent *event);

private:
int m_pin;
int m_id;
bool m_blink;
int m_id;
bool m_blink;


bool m_blink;
void timerEvent(QTimerEvent *event);

private:
int m_pin;
int m_id;
bool m_blink;
int m_id;
bool m_blink;


bool m_blink;
void timerEvent(QTimerEvent *event);

private:
int m_pin;
int m_id;
bool m_blink;
int m_id;
bool m_blink;

void timerEvent(QTimerEvent *event);

private:
int m_pin;
int m_id;
bool m_blink;
int m_id;
bool m_blink;

void timerEvent(QTimerEvent *event);

private:
int m_pin;
int m_id;
bool m_blink;
int m_id;
bool m_blink;

void timerEvent(QTimerEvent *event);

private:
int m_pin;
int m_id;
bool m_blink;
int m_id;
bool m_blink;


    void setPin( unsigned int pin);

    void setOn( int blink = 0); // blink in millisec
    void setOff();


};
